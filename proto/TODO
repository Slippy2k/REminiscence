cutscenes
	scale2x

versions
	android : libfb.so fb-jni
	pc/sdl : libfb.so fb
	rendre commun le code opengl(es) - #ifdef GLES
	interface C pour le shared object
		stubInit
		stubQuit
		stubQueueInput
		stubDoTick
		stubInitGL - onSurfaceCreated
		stubDrawGL - printFps

menu
	choix du niveau/difficulte par menu "texte"

sounds
	decode sega vce segments to .wav (N-PART.wav)
	.mp3 as background music
	mac sounds

touch UI
	'menu' hard button -> trigger inventory
	- click on inventory icon -> enter
	- click on gun -> space
	action (right area) -> shift
	virtual pad

texture rendering
	glColor(1., 0., 0.); niveau 2
	inventory display background (saveGfxList)
	scale2x au load ?

