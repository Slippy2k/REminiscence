cutscenes
	scale2x

sounds
	decode sega vce segments to .wav (N-PART.wav)
	.mp3 as background music
	mac sounds

touch UI
	long press on inventory icon -> trigger inventory
	click on inventory icon -> use/Return
	click on gun -> space
	action -> shift
	virtual pad

texture rendering
	erase : stencil ?
	font
	scale2x au load ?
	TextureCache.Image : avoir 2 entrees, une pour la texture (refcount), une pour l'image - gestion de N meme images aux positions differentes

