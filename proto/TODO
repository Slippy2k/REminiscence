scaler2x pour les cutscenes
decode sega vce segments to .wav (N-PART.wav) ; .mp3 as background music
mouse UI
	long press on inventory icon -> trigger inventory
	click on inventory icon -> use/Return
	click on gun -> space
	action -> shift
virtual pad

Android
	rendu par texture
		une texture pour le background
		N textures pour les sprites
	TextureCache
	load en 565 (background) + 555_1 (sprites)
	drawList
		erase : stencil, zbuffer ?
		xflip : glMatrix
