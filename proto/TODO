LD_LIBRARY_PATH=$(pwd) ./fb ../DATA/Flashback.bin
./fb ~/Documents/Data/flashback/Flashback.bin

render.cpp
	cleanup texture handling
	move MAC data conversion to convert_data.h

cleanup texture handling (one texture for background, one texture for gameplay ?)
libSDL2 - android + gamepad support
	remove USE_GLES

issues
	text story color does not match speaker

sprites, icone, fonte dans une texture (texturepacker)

split game.cpp
	hotspot.cpp
	drawable.cpp - Gfx*

integrate with 'google play games' service to save gamestates in cloud (need screenshot)

cutscenes
	scale2x

convert_data
	background dans un .tga ou .bmp (RLE ou zlib)
	bundle format - mac resource fork

convert_mac_png
	output paletted .png

texture rendering
	inventory display background (saveGfxList)
	visual feedback on user inputs
